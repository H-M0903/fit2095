//create a new function to add all the odd numbers 
//between 0 to N inclusive 
var arr = [1,2,3,4,5,6,7,8,9];

enqueue(newItem){
    this.q.push(newItem) //so we can add something new to the array 
};

function oddNum (n) {
    for(let i =0;i<=n;i++){
        if(i%2==1){
            this.q.enqueue(i)
        }
    }
    
};
